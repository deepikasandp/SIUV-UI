import React from 'react';
import AppRoutes from './routes/AppRoutes'; // Import the AppRoutes component

const App: React.FC = () => {
  return (
    <div>
      <AppRoutes />
    </div>
  );
};

export default App;
